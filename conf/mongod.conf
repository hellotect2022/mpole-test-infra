# mongod.conf

systemLog:
  destination: file
  path: /data/log/mongod.log
  logAppend: true

storage:
  dbPath: /data/db

net:
  port: 27017
  bindIp: 0.0.0.0   # 외부 접속 허용

security:
  authorization: enabled

#replication:
  #replSetName: rs0
